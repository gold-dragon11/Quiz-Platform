import { Injectable } from '@nestjs/common';

/**
 * Authentication use cases — registration, login, token refresh, logout, email
 * verification, and password reset (docs/06-backend/authentication.md §2) — are
 * implemented in the phases that follow.
 *
 * This phase establishes the module and its infrastructure only, so the service
 * is intentionally empty.
 */
@Injectable()
export class AuthService {}
