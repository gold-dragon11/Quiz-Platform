/**
 * The Statistics page is a read-only view over the Statistics API and takes no
 * user input, so there are no schemas to validate. This module exists only to
 * keep the feature structure consistent with the other features.
 */
export {};
