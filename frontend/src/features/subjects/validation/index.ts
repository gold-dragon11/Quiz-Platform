/**
 * The Subjects browser takes no server-bound user input — search is entirely
 * client-side — so there are no schemas to validate. This module exists only
 * to keep the feature structure consistent with the other features.
 */
export {};
